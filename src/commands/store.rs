use crate::cli::StoreCommand;
use crate::error::Result;

pub async fn handle_store(_subcommand: StoreCommand) -> Result<()> {
    // Stub — full implementation provided by the store commands worker.
    todo!("store command handler not yet implemented")
}
